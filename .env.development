# 开发模式默认配置（仅 `npm run dev` 生效；生产构建/CI 使用 production 模式，忽略本文件）
#
# 开箱即用：默认启用 MSW 本地 mock + 演示模式，后端未启动也可完整浏览体验核心闭环。
# - VITE_ENABLE_MSW=true：启用 MSW Service Worker 拦截 /api，返回本地 mock 数据。
# - VITE_DEMO_MODE=true：允许「快捷登录」与 mock 回退（IS_DEMO_MODE=true）。
#
# 需要联调真实后端时，在根目录创建 .env.development.local 覆盖：
#   VITE_ENABLE_MSW=false
#   VITE_API_PROXY_TARGET=http://localhost:8080
VITE_ENABLE_MSW=true
VITE_DEMO_MODE=true
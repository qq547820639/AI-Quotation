[pytest]
# P2-14 Task 19：覆盖率质量门禁
# 对所有 app 模块统计覆盖率，未达 80% 即失败（CI pipeline 失败）。
# 单独运行：pytest --no-cov 可关闭覆盖率。
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=80
testpaths = tests
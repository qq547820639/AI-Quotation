fastapi==0.141.1
uvicorn[standard]==0.34.2
sqlalchemy==2.0.36
pydantic==2.9.2
pytest==9.0.3
pytest-cov==7.1.0
httpx==0.28.1
bcrypt==4.2.1
alembic==1.14.0
python-multipart==0.0.31
# 传递依赖安全修复（pip-audit 门禁）：
# - starlette 由 fastapi>=0.128 引入（修复 CVE-2024-47874 等系列）
# - urllib3>=2 修复 botocore/requests 拉入的 1.26.x CVE
# - pillow>=12 修复 reportlab 拉入的 11.x 系列 CVE
# - click>=8.3.3 修复 uvicorn/celery 拉入的 8.1.x CVE
# - python-dotenv>=1.2.2 修复 uvicorn[standard] 拉入的 1.2.1 CVE
urllib3>=2.7.0
pillow>=12.2.0
click>=8.3.3
python-dotenv>=1.2.2
# PostgreSQL 驱动（生产环境；DATABASE_URL 形如 postgresql://... 时使用）
psycopg2-binary==2.9.10

# P2-12 Task 17：服务端导出（Excel / PDF）
openpyxl==3.1.5
reportlab==5.0.0

# P0：Redis 客户端（REDIS_URL 配置时用于限流/幂等/缓存/token，可多实例）
redis==5.0.8
# P0：对象存储（S3/MinIO，S3_* 配置完整时用于附件存储）
boto3==1.35.0

# P1：持久化任务队列（outbox + Celery，broker 复用 Redis）
celery[redis]==5.4.0

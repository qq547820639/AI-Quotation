# Tasks

> 按用户要求的 9 阶段推进，优先生产阻断项（认证/数据一致性/并发/编号），再体验增强。遵循"保留当前技术栈、不掩盖未实现逻辑、所有文案进 i18n、每批运行对应测试修复回归"。执行前先逐项核对文档与最新代码，不假定已实现。

## 第一阶段：生产安全与鉴权（P0）
- [ ] Task 1: 后端安全密码认证
  - [ ] 1.1 引入 bcrypt（`passlib` 或 `bcrypt`），`User` 增加 `password_hash` 字段；`seed.py` 为演示账号写入哈希密码
  - [ ] 1.2 `POST /api/auth/login` 请求体增加 `password`；校验密码哈希；用户不存在与密码错误返回一致 401 文案（不泄露用户是否存在）
  - [ ] 1.3 `Token` 增加 `expires_at`；登录时设置过期时间；`get_current_user` 校验过期并清理失效 token
  - [ ] 1.4 登出撤销：`logout` 删除当前 token（而非全部）；新增按 token 撤销能力
  - [ ] 1.5 连续失败登录速率限制（内存/DB 计数，失败超阈值返回 429 或暂时 429）
  - [ ] 1.6 演示账号快捷登录仅当 `APP_DEMO_MODE=true`（开发/演示）时启用；生产默认关闭
  - [ ] 1.7 新增 `config.py` 环境变量（`APP_DEMO_MODE`、`TOKEN_TTL`、`LOGIN_MAX_ATTEMPTS` 等）
- [ ] Task 2: 前端 401 处理与认证对接
  - [ ] 2.1 登录页面增加密码输入；`useAuthStore.login` 传 `{userId, password}`；演示模式（无密码）兼容
  - [ ] 2.2 401 处理：清除 token + 清除用户/权限状态 + 避免重复弹多个错误 + 保存安全返回地址 + 跳转登录页 + 登录后回原页面
  - [ ] 2.3 密码哈希后演示账号与真实账号路径统一（`AuthAdapter` 复用）
- [ ] Task 3: 后端路由权限审查与测试
  - [ ] 3.1 核对所有路由的认证/角色/资源级权限，补充缺失的 `require_permission`
  - [ ] 3.2 新增权限测试：登录成败、token 过期/撤销、RBAC、资源级权限、401/403

## 第二阶段：API、数据一致性与错误恢复（P0）
- [ ] Task 4: 移除静默失败并对齐写操作流程
  - [ ] 4.1 全项目扫描并移除空 `catch`、`.catch(() => {})`、静默失败
  - [ ] 4.2 写操作统一 pending/success/error 状态，服务端确认后再提示成功，乐观更新回滚，防重复提交（复用 `writeResult.ts` 模式扩展到所有 store）
- [ ] Task 5: 供应商定标数据完整性
  - [ ] 5.1 修复 `selected_supplier_map` 更新：改为完整映射或增量 PATCH，不得只提交当前物料覆盖其他物料
  - [ ] 5.2 新增多物料选择与重复修改的回归测试（前端 store + 后端 API）
- [ ] Task 6: 并发控制（版本号 + 409）
  - [ ] 6.1 `Inquiry` 增加 `version` 字段；更新/动作接口携带 `version`，不匹配返回 409
  - [ ] 6.2 前端 409 处理：展示冲突内容，提供重新加载或重新应用选择
  - [ ] 6.3 新增并发冲突测试（后端 + 前端 store）
- [ ] Task 7: 询价编号服务端生成
  - [ ] 7.1 后端 `create_inquiry` 生成唯一编号（DB 唯一约束），并发碰撞时事务内重试
  - [ ] 7.2 前端不再本地生成 `code`，创建成功后使用服务端返回的 `code`
  - [ ] 7.3 新增唯一编号与并发碰撞测试
- [ ] Task 8: Axios 封装与错误模型
  - [ ] 8.1 仅 GET/HEAD 有限重试；写请求不盲目重试；确需重试的写请求用 idempotency key
  - [ ] 8.2 超时、取消请求、重复请求合并
  - [ ] 8.3 后端错误统一转为前端可消费错误模型（复用 `ApiError`）
  - [ ] 8.4 明确服务端状态与本地草稿边界，本地数据不长期伪装成已保存

## 第三阶段：加载态、空状态、保存态和失败态
- [ ] Task 9: 统一状态模型（页面）
  - [ ] 9.1 为询价列表/详情、报价对比、Dashboard、审批页面建立 idle/loading/refreshing/success/empty/saving/error/offline 状态模型
  - [ ] 9.2 修复 Dashboard 无数据永久 Skeleton；Skeleton 仅用于首次加载，空数据用 Empty
  - [ ] 9.3 图表/表格/卡片/详情页统一空状态与错误状态组件，错误状态提供"重新加载"
  - [ ] 9.4 局部刷新保留已有数据，避免整页闪烁
- [ ] Task 10: 评论保存与导出反馈
  - [ ] 10.1 评论保存前 trim、显示"正在保存/已保存/保存失败"、服务端确认后提示成功、避免旧请求覆盖新内容
  - [ ] 10.2 PDF/Excel 导出：导出中状态、防重复点击、成功/失败提示、大数据量保护

## 第四阶段：报价对比与核心采购体验
- [ ] Task 11: 报价对比页优化
  - [ ] 11.1 清理不合理 `useEffect` 依赖与 lint 禁用
  - [ ] 11.2 防止询价/报价刷新或路由切换覆盖未提交选择与评论
  - [ ] 11.3 区分供应商选择/单项推荐/整体推荐/人工决定；用户选择非推荐供应商时可填原因（不强制）
  - [ ] 11.4 对异常/缺失/超预算/交期/付款风险给出可解释提示
  - [ ] 11.5 在真实性能分析证明有收益时才用 memo/useMemo/useCallback；必要时分页/虚拟滚动/分段渲染
  - [ ] 11.6 修复数组索引 key、陈旧闭包、重复计算问题

## 第五阶段：列表、筛选与移动端体验
- [ ] Task 12: 表格列设置与批量操作
  - [ ] 12.1 自定义列设置组件（不依赖不存在的 `columnSetting`）：列显示/隐藏、列宽、固定列、密度、排序、批量选择、批量操作、恢复默认
  - [ ] 12.2 筛选/排序/分页/搜索持久化到 URL 或本地，返回页面可恢复
  - [ ] 12.3 "清除所有筛选" + 展示当前生效条件
- [ ] Task 13: 移动端适配
  - [ ] 13.1 375/768/桌面宽度：关键按钮不越界、筛选可收起、表格可读（卡片/横向滚动）、固定操作区不遮挡、弹窗/抽屉完整可操作

## 第六阶段：无障碍与键盘操作
- [ ] Task 14: a11y 改造
  - [ ] 14.1 语义化按钮/链接/标题/表单标签/表格；可点击 `div` 改造为按钮
  - [ ] 14.2 键盘可操作、清晰焦点、弹窗焦点管理、关闭后焦点返回触发元素
  - [ ] 14.3 图标/状态/图表/异步状态加 ARIA 辅助文本；文本与状态颜色对比度不依赖单一颜色
  - [ ] 14.4 引入 axe 无障碍检查覆盖核心页面，无严重级问题

## 第七阶段：AI 服务真实化和可降级设计
- [ ] Task 15: AI 服务抽象与降级
  - [ ] 15.1 抽象 `AIBackend`/`LocalRuleBackend`/`RemoteAIBackend`，环境变量选择后端
  - [ ] 15.2 界面显示当前模式（本地规则/远程 AI/降级/AI 不可用）；不通过固定延时制造"思考"假象
  - [ ] 15.3 远程调用带超时/取消/错误/限流/敏感信息过滤/可控日志/用户确认
  - [ ] 15.4 AI 输出结构校验，不直接信任自由文本；最终定标保留人工确认
  - [ ] 15.5 为本地规则与远程后端分别补测试

## 第八阶段：后端工程化与可观测性
- [ ] Task 16: 数据库迁移 + 可观测性
  - [ ] 16.1 引入 Alembic 并生成可验证的首个迁移（保留自动建表作为兼容/测试路径）
  - [ ] 16.2 结构化日志 + request ID；前端错误/后端日志/Sentry 通过 request ID 关联
  - [ ] 16.3 区分用户输入/权限/数据冲突/服务不可用/未知系统错误；健康检查与就绪检查
  - [ ] 16.4 日志不记录密码/完整 token/敏感采购数据；事务边界保证不产生半完成数据

## 第九阶段：自动化测试与 CI 门禁
- [ ] Task 17: 测试补齐
  - [ ] 17.1 前端：API client、401 退出跳转、Store 成功/失败/回滚/并发、Dashboard 空态、评论保存、多物料定标、报价异常、中英切换、移动端关键交互
  - [ ] 17.2 后端 pytest：登录成败、token 过期/撤销、RBAC、资源级权限、状态流转、唯一编号、并发冲突、事务回滚、非法输入、多物料定标完整性
  - [ ] 17.3 Playwright 增加真实后端模式（不全部依赖 MSW），覆盖完整主流程并验证刷新后数据正确
- [ ] Task 18: CI 门禁与文档同步
  - [ ] 18.1 CI 实际执行 npm ci + ESLint + tsc + Vitest + build + pytest + Playwright 真实后端流程；失败输出日志与截图/trace
  - [ ] 18.2 生成 `.env.example`；更新 README/部署文档/迁移说明/测试说明与环境变量示例
  - [ ] 18.3 最终验证：lint/typecheck/vitest/build/pytest/Docker/E2E；输出修改文件清单、修复说明、迁移说明、命令结果、测试清单、未解决问题、旧数据风险、人工验收步骤

# Task Dependencies
- Task 1 独立先行（认证是生产安全基础）
- Task 2 依赖 Task 1（前端对接后端密码认证）
- Task 3 依赖 Task 1
- Task 4 依赖 Task 2（前端写操作流程）
- Task 5 依赖 Task 4（定标走可靠写流程）
- Task 6 依赖 Task 4（并发冲突前端处理）
- Task 7 独立（后端编号生成）
- Task 8 依赖 Task 2 与 4（client 错误模型）
- Task 9 依赖 Task 4（状态模型）
- Task 10 依赖 Task 9
- Task 11 依赖 Task 9/10（对比页状态与评论）
- Task 12/13 依赖 Task 9（列表状态）
- Task 14 依赖 Task 11/12/13（核心页面）
- Task 15 独立（AI 抽象）
- Task 16 依赖 Task 1（迁移含 auth 字段）
- Task 17 依赖全部业务改动
- Task 18 依赖 Task 17